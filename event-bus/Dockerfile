FROM node:alpine

ENV POSTS_PORT=4000

ENV COMMENTS_PORT=4001

ENV MODERATION_PORT=4002

ENV QUERY_PORT=4003

ENV EVENT_BUS_PORT=4005

WORKDIR /app

COPY package.json ./

RUN npm install

COPY ./ ./

CMD ["npm", "start"]
